services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: sca
      POSTGRES_USER: sca
      POSTGRES_PASSWORD: sca
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
